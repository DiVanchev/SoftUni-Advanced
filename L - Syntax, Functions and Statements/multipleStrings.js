function multiStrings (s1, s2, s3){
    console.log(s1.length + s2.length + s3.length);
    console.log(Math.floor((s1.length + s2.length + s3.length) / 3));   
}
multiStrings('chocolate', 'ice cream', 'cake');